import { StyleSheet } from 'react-native';

const style = (width,height,isPortrait) => StyleSheet.create({
    title: {
        fontSize: 22, fontFamily: 'Lato-Bold',
    },
});

export default style;
